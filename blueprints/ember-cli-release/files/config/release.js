/* jshint node:true */

// For details on each option run `ember help release`
module.exports = {
  // local: true,
  // remote: 'some_remote',
  // annotation: "Release %@",
  // strategy: 'date',
  // format: 'YYYY-MM-DD',
  // timezone: 'America/Los_Angeles',
  // build: function() { ... }
  // preTag: function() { ... }
  // publish: funciton() { ... }
};